<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Active CSS Robot</title>
    <style>
        :root {
            --robot-white: #ffffff;
            --robot-blue: #4a90e2;
            --robot-pink: #ff8fa3;
            --bg-color: #d9e9f7;
            --shadow: rgba(0, 0, 0, 0.1);
        }

        body {
            background-color: var(--bg-color);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        /* Container for positioning */
        .stage {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Main Robot Body floating animation */
        .robot {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            animation: float 3s ease-in-out infinite;
        }

        /* Head & Antenna */
        .head {
            width: 140px;
            height: 100px;
            background: var(--robot-white);
            border-radius: 60px 60px 35px 35px;
            position: relative;
            border-bottom: 5px solid #e0e6ed;
            z-index: 10;
            display: flex;
            justify-content: center;
            animation: tilt 6s ease-in-out infinite;
        }

        .antenna {
            width: 4px;
            height: 20px;
            background: var(--robot-blue);
            position: absolute;
            top: -18px;
        }

        .antenna::after {
            content: '';
            width: 12px;
            height: 12px;
            background: var(--robot-pink);
            border-radius: 50%;
            position: absolute;
            top: -10px;
            left: -4px;
            box-shadow: 0 0 10px var(--robot-pink);
        }

        .ears {
            width: 160px;
            height: 40px;
            background: var(--robot-pink);
            position: absolute;
            top: 35px;
            border-radius: 10px;
            z-index: -1;
        }

        /* Eyes and Mouth */
        .face {
            margin-top: 30px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .eyes-container {
            display: flex;
            gap: 15px;
        }

        .eye {
            width: 38px;
            height: 38px;
            background: var(--robot-blue);
            border-radius: 50%;
            position: relative;
            overflow: hidden; /* For blink effect */
        }

        /* White Shine in Eye */
        .eye::after {
            content: '';
            position: absolute;
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            top: 8px;
            left: 10px;
        }

        /* Blink Overlay */
        .eye::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: var(--robot-white);
            top: -100%;
            z-index: 2;
            animation: blink 5s infinite;
        }

        .mouth {
            width: 14px;
            height: 7px;
            border-bottom: 3px solid #555;
            border-radius: 50%;
            margin-top: 5px;
        }

        /* Body & Screen */
        .body {
            width: 120px;
            height: 105px;
            background: var(--robot-white);
            border-radius: 25px;
            margin-top: 8px;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            border-bottom: 5px solid #e0e6ed;
        }

        .screen {
            width: 85px;
            height: 45px;
            border: 4px solid var(--robot-blue);
            border-radius: 12px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            font-size: 11px;
            font-weight: bold;
            color: var(--robot-blue);
            background: #fdfdfd;
        }

        /* Arms */
        .arm {
            width: 22px;
            height: 55px;
            background: var(--robot-blue);
            position: absolute;
            top: 15px;
            border-radius: 15px;
            transform-origin: top center;
        }

        .left-arm { 
            left: -28px; 
            animation: swingLeft 3s ease-in-out infinite;
        }
        .right-arm { 
            right: -28px; 
            animation: swingRight 3s ease-in-out infinite;
        }

        /* Legs */
        .legs {
            display: flex;
            gap: 25px;
        }

        .leg {
            width: 28px;
            height: 30px;
            background: var(--robot-blue);
            border-radius: 0 0 10px 10px;
            border-top: 4px solid #357abd;
        }

        /* Shadow beneath robot */
        .shadow {
            width: 110px;
            height: 12px;
            background: var(--shadow);
            border-radius: 50%;
            margin-top: 30px;
            animation: shadowPulse 3s ease-in-out infinite;
        }

        /* ANIMATIONS */
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        @keyframes shadowPulse {
            0%, 100% { transform: scale(1); opacity: 0.4; }
            50% { transform: scale(0.7); opacity: 0.1; }
        }

        @keyframes blink {
            0%, 96%, 100% { top: -100%; }
            98% { top: 0%; }
        }

        @keyframes tilt {
            0%, 100% { transform: rotate(0deg); }
            25% { transform: rotate(2deg); }
            75% { transform: rotate(-2deg); }
        }

        @keyframes swingLeft {
            0%, 100% { transform: rotate(15deg); }
            50% { transform: rotate(25deg); }
        }

        @keyframes swingRight {
            0%, 100% { transform: rotate(-15deg); }
            50% { transform: rotate(-25deg); }
        }
    </style>
</head>
<body>

    <div class="stage">
        <div class="robot">
            <div class="head">
                <div class="antenna"></div>
                <div class="ears"></div>
                <div class="face">
                    <div class="eyes-container">
                        <div class="eye"></div>
                        <div class="eye"></div>
                    </div>
                    <div class="mouth"></div>
                </div>
            </div>

            <div class="body">
                <div class="arm left-arm"></div>
                <div class="screen">HELLO! :c)</div>
                <div class="arm right-arm"></div>
            </div>

            <div class="legs">
                <div class="leg"></div>
                <div class="leg"></div>
            </div>
        </div>

        <div class="shadow"></div>
    </div>

</body>
</html>
